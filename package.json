{
  "name": "version-vector-set",
  "version": "1.0.0",
  "description": "Offline-replication version vector set with contiguous prefixes, canonical gap intervals and bounded missing requests.",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --expose-gc --test dist/test/"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.6.0"
  }
}
