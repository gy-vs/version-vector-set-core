{
  "name": "offline-version-vectors",
  "version": "1.0.0",
  "description": "Version-vector collection with contiguous prefix + discrete interval segments for offline replication",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/",
    "fuzz": "npm run build && node scripts/fuzz-add.mjs && node scripts/fuzz-merge.mjs && node scripts/fuzz-diff.mjs && node scripts/fuzz-heavy.mjs"
  },
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^5.6.0"
  }
}
